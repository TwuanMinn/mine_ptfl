import React from 'react';

export const SectionDivider = ({ darkMode }) => {
    return (
        <div className="section-divider-gradient" aria-hidden="true">
            {/* Decorative gradient line with centered glowing star */}
        </div>
    );
};

export default SectionDivider;
